# Copyright (c) 2016, 2026, Oracle and/or its affiliates.  All rights reserved.
# This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.

# NOTE: This class is auto generated by OracleSDKGenerator. DO NOT EDIT. API Version: 20190415
require 'date'
require 'logger'

# rubocop:disable Lint/UnneededCopDisableDirective, Metrics/LineLength
module OCI
  # The details of the last successfully processed controlled update action.
  class Mysql::Models::ControlledUpdate
    TARGET_DB_INSTANCES_ENUM = [
      TARGET_DB_INSTANCES_ALL_BUT_PRIMARY = 'ALL_BUT_PRIMARY'.freeze,
      TARGET_DB_INSTANCES_PRIMARY_ONLY = 'PRIMARY_ONLY'.freeze,
      TARGET_DB_INSTANCES_UNKNOWN_ENUM_VALUE = 'UNKNOWN_ENUM_VALUE'.freeze
    ].freeze

    # The MySQL version to be applied to the selected instances.
    # @return [String]
    attr_accessor :target_mysql_version

    # Defines the MySQL instances to be operated during a controlled update.
    #  - ALL_BUT_PRIMARY: Update all MySQL instances in a highly available DB System except the primary group member,
    #    without triggering a controlled failover.
    #  - PRIMARY_ONLY: Update the primary group member in a highly available DB System
    #    after a controlled failover (downtime is expected). This operation requires that the other
    #    MySQL instances have been previously updated using the ALL_BUT_PRIMARY option.
    #
    # @return [String]
    attr_reader :target_db_instances

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        # rubocop:disable Style/SymbolLiteral
        'target_mysql_version': :'targetMysqlVersion',
        'target_db_instances': :'targetDbInstances'
        # rubocop:enable Style/SymbolLiteral
      }
    end

    # Attribute type mapping.
    def self.swagger_types
      {
        # rubocop:disable Style/SymbolLiteral
        'target_mysql_version': :'String',
        'target_db_instances': :'String'
        # rubocop:enable Style/SymbolLiteral
      }
    end

    # rubocop:disable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity
    # rubocop:disable Metrics/MethodLength, Layout/EmptyLines, Style/SymbolLiteral


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    # @option attributes [String] :target_mysql_version The value to assign to the {#target_mysql_version} property
    # @option attributes [String] :target_db_instances The value to assign to the {#target_db_instances} property
    def initialize(attributes = {})
      return unless attributes.is_a?(Hash)

      self.target_mysql_version = attributes[:'targetMysqlVersion'] if attributes[:'targetMysqlVersion']

      raise 'You cannot provide both :targetMysqlVersion and :target_mysql_version' if attributes.key?(:'targetMysqlVersion') && attributes.key?(:'target_mysql_version')

      self.target_mysql_version = attributes[:'target_mysql_version'] if attributes[:'target_mysql_version']

      self.target_db_instances = attributes[:'targetDbInstances'] if attributes[:'targetDbInstances']

      raise 'You cannot provide both :targetDbInstances and :target_db_instances' if attributes.key?(:'targetDbInstances') && attributes.key?(:'target_db_instances')

      self.target_db_instances = attributes[:'target_db_instances'] if attributes[:'target_db_instances']
    end
    # rubocop:enable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity
    # rubocop:enable Metrics/MethodLength, Layout/EmptyLines, Style/SymbolLiteral

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] target_db_instances Object to be assigned
    def target_db_instances=(target_db_instances)
      # rubocop:disable Style/ConditionalAssignment
      if target_db_instances && !TARGET_DB_INSTANCES_ENUM.include?(target_db_instances)
        OCI.logger.debug("Unknown value for 'target_db_instances' [" + target_db_instances + "]. Mapping to 'TARGET_DB_INSTANCES_UNKNOWN_ENUM_VALUE'") if OCI.logger
        @target_db_instances = TARGET_DB_INSTANCES_UNKNOWN_ENUM_VALUE
      else
        @target_db_instances = target_db_instances
      end
      # rubocop:enable Style/ConditionalAssignment
    end

    # rubocop:disable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity, Layout/EmptyLines


    # Checks equality by comparing each attribute.
    # @param [Object] other the other object to be compared
    def ==(other)
      return true if equal?(other)

      self.class == other.class &&
        target_mysql_version == other.target_mysql_version &&
        target_db_instances == other.target_db_instances
    end
    # rubocop:enable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity, Layout/EmptyLines

    # @see the `==` method
    # @param [Object] other the other object to be compared
    def eql?(other)
      self == other
    end

    # rubocop:disable Metrics/AbcSize, Layout/EmptyLines


    # Calculates hash code according to all attributes.
    # @return [Fixnum] Hash code
    def hash
      [target_mysql_version, target_db_instances].hash
    end
    # rubocop:enable Metrics/AbcSize, Layout/EmptyLines

    # rubocop:disable Metrics/AbcSize, Layout/EmptyLines


    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)

      self.class.swagger_types.each_pair do |key, type|
        if type =~ /^Array<(.*)>/i
          # check to ensure the input is an array given that the the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            public_method("#{key}=").call(
              attributes[self.class.attribute_map[key]]
                .map { |v| OCI::Internal::Util.convert_to_type(Regexp.last_match(1), v) }
            )
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          public_method("#{key}=").call(
            OCI::Internal::Util.convert_to_type(type, attributes[self.class.attribute_map[key]])
          )
        end
        # or else data not found in attributes(hash), not an issue as the data can be optional
      end

      self
    end
    # rubocop:enable Metrics/AbcSize, Layout/EmptyLines

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = public_method(attr).call
        next if value.nil? && !instance_variable_defined?("@#{attr}")

        hash[param] = _to_hash(value)
      end
      hash
    end

    private

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
# rubocop:enable Lint/UnneededCopDisableDirective, Metrics/LineLength
