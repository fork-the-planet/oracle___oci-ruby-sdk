# Copyright (c) 2016, 2026, Oracle and/or its affiliates.  All rights reserved.
# This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.

# NOTE: This class is auto generated by OracleSDKGenerator. DO NOT EDIT. API Version: 20190101
require 'date'
require 'logger'
require_relative 'schedule_trigger'

# rubocop:disable Lint/UnneededCopDisableDirective, Metrics/LineLength
module OCI
  # The interval schedule trigger.
  class DataScience::Models::ScheduleIntervalTrigger < DataScience::Models::ScheduleTrigger
    FREQUENCY_ENUM = [
      FREQUENCY_HOURLY = 'HOURLY'.freeze,
      FREQUENCY_DAILY = 'DAILY'.freeze,
      FREQUENCY_UNKNOWN_ENUM_VALUE = 'UNKNOWN_ENUM_VALUE'.freeze
    ].freeze

    # **[Required]** The type of frequency
    # @return [String]
    attr_reader :frequency

    # **[Required]** The interval of frequency.
    # @return [Integer]
    attr_accessor :interval

    # Maximum number of minutes after timeStart that the scheduler may use to randomly select the first execution time.
    # This value is considered only when isRandomStartTime is true.
    # This value applies only to the initial execution; subsequent executions remain deterministic based on the resolved first trigger time.
    # If timeStart is null, the service resolves the effective start time using the current time.
    # The initial jitter window is then applied once to that resolved start time to determine the first execution time.
    # If not provided and isRandomStartTime is true, the service defaults the jitter window to half of the configured interval duration.
    # The value must not exceed the configured interval duration.
    #
    # @return [Integer]
    attr_accessor :initial_jitter_in_minutes

    # when true, system generates a randomized first start time between timeStart and timeStart + initialJitterInMinutes.
    # if timeStart is null, the current time is used as the base start time.
    #
    # @return [BOOLEAN]
    attr_accessor :is_random_start_time

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        # rubocop:disable Style/SymbolLiteral
        'trigger_type': :'triggerType',
        'time_start': :'timeStart',
        'time_end': :'timeEnd',
        'frequency': :'frequency',
        'interval': :'interval',
        'initial_jitter_in_minutes': :'initialJitterInMinutes',
        'is_random_start_time': :'isRandomStartTime'
        # rubocop:enable Style/SymbolLiteral
      }
    end

    # Attribute type mapping.
    def self.swagger_types
      {
        # rubocop:disable Style/SymbolLiteral
        'trigger_type': :'String',
        'time_start': :'DateTime',
        'time_end': :'DateTime',
        'frequency': :'String',
        'interval': :'Integer',
        'initial_jitter_in_minutes': :'Integer',
        'is_random_start_time': :'BOOLEAN'
        # rubocop:enable Style/SymbolLiteral
      }
    end

    # rubocop:disable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity
    # rubocop:disable Metrics/MethodLength, Layout/EmptyLines, Style/SymbolLiteral


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    # @option attributes [DateTime] :time_start The value to assign to the {OCI::DataScience::Models::ScheduleTrigger#time_start #time_start} proprety
    # @option attributes [DateTime] :time_end The value to assign to the {OCI::DataScience::Models::ScheduleTrigger#time_end #time_end} proprety
    # @option attributes [String] :frequency The value to assign to the {#frequency} property
    # @option attributes [Integer] :interval The value to assign to the {#interval} property
    # @option attributes [Integer] :initial_jitter_in_minutes The value to assign to the {#initial_jitter_in_minutes} property
    # @option attributes [BOOLEAN] :is_random_start_time The value to assign to the {#is_random_start_time} property
    def initialize(attributes = {})
      return unless attributes.is_a?(Hash)

      attributes['triggerType'] = 'INTERVAL'

      super(attributes)

      # convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h| h[k.to_sym] = v }

      self.frequency = attributes[:'frequency'] if attributes[:'frequency']

      self.interval = attributes[:'interval'] if attributes[:'interval']

      self.initial_jitter_in_minutes = attributes[:'initialJitterInMinutes'] if attributes[:'initialJitterInMinutes']

      raise 'You cannot provide both :initialJitterInMinutes and :initial_jitter_in_minutes' if attributes.key?(:'initialJitterInMinutes') && attributes.key?(:'initial_jitter_in_minutes')

      self.initial_jitter_in_minutes = attributes[:'initial_jitter_in_minutes'] if attributes[:'initial_jitter_in_minutes']

      self.is_random_start_time = attributes[:'isRandomStartTime'] unless attributes[:'isRandomStartTime'].nil?
      self.is_random_start_time = false if is_random_start_time.nil? && !attributes.key?(:'isRandomStartTime') # rubocop:disable Style/StringLiterals

      raise 'You cannot provide both :isRandomStartTime and :is_random_start_time' if attributes.key?(:'isRandomStartTime') && attributes.key?(:'is_random_start_time')

      self.is_random_start_time = attributes[:'is_random_start_time'] unless attributes[:'is_random_start_time'].nil?
      self.is_random_start_time = false if is_random_start_time.nil? && !attributes.key?(:'isRandomStartTime') && !attributes.key?(:'is_random_start_time') # rubocop:disable Style/StringLiterals
    end
    # rubocop:enable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity
    # rubocop:enable Metrics/MethodLength, Layout/EmptyLines, Style/SymbolLiteral

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] frequency Object to be assigned
    def frequency=(frequency)
      # rubocop:disable Style/ConditionalAssignment
      if frequency && !FREQUENCY_ENUM.include?(frequency)
        OCI.logger.debug("Unknown value for 'frequency' [" + frequency + "]. Mapping to 'FREQUENCY_UNKNOWN_ENUM_VALUE'") if OCI.logger
        @frequency = FREQUENCY_UNKNOWN_ENUM_VALUE
      else
        @frequency = frequency
      end
      # rubocop:enable Style/ConditionalAssignment
    end

    # rubocop:disable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity, Layout/EmptyLines


    # Checks equality by comparing each attribute.
    # @param [Object] other the other object to be compared
    def ==(other)
      return true if equal?(other)

      self.class == other.class &&
        trigger_type == other.trigger_type &&
        time_start == other.time_start &&
        time_end == other.time_end &&
        frequency == other.frequency &&
        interval == other.interval &&
        initial_jitter_in_minutes == other.initial_jitter_in_minutes &&
        is_random_start_time == other.is_random_start_time
    end
    # rubocop:enable Metrics/CyclomaticComplexity, Metrics/AbcSize, Metrics/PerceivedComplexity, Layout/EmptyLines

    # @see the `==` method
    # @param [Object] other the other object to be compared
    def eql?(other)
      self == other
    end

    # rubocop:disable Metrics/AbcSize, Layout/EmptyLines


    # Calculates hash code according to all attributes.
    # @return [Fixnum] Hash code
    def hash
      [trigger_type, time_start, time_end, frequency, interval, initial_jitter_in_minutes, is_random_start_time].hash
    end
    # rubocop:enable Metrics/AbcSize, Layout/EmptyLines

    # rubocop:disable Metrics/AbcSize, Layout/EmptyLines


    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)

      self.class.swagger_types.each_pair do |key, type|
        if type =~ /^Array<(.*)>/i
          # check to ensure the input is an array given that the the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            public_method("#{key}=").call(
              attributes[self.class.attribute_map[key]]
                .map { |v| OCI::Internal::Util.convert_to_type(Regexp.last_match(1), v) }
            )
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          public_method("#{key}=").call(
            OCI::Internal::Util.convert_to_type(type, attributes[self.class.attribute_map[key]])
          )
        end
        # or else data not found in attributes(hash), not an issue as the data can be optional
      end

      self
    end
    # rubocop:enable Metrics/AbcSize, Layout/EmptyLines

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = public_method(attr).call
        next if value.nil? && !instance_variable_defined?("@#{attr}")

        hash[param] = _to_hash(value)
      end
      hash
    end

    private

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
# rubocop:enable Lint/UnneededCopDisableDirective, Metrics/LineLength
